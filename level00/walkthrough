# Значения параметров gdb 

sub - выделение памяти под исполняемую программу
call - вызов функции (название указано в <name>)
jne - jump if not equals - аналог "если не равно" (if !=)
cmp - сравнение значений

уязвимость printf 
(level05) Если передавать указатель на переменую,
то он по этому указателю в памяти положит число,
соответствующее кол-ву выводимых символов.

Описание регистров ассемблера
https://yutewiyof.gitbook.io/intro-rev-ida-pro/chast-03


> gdb ./level00
(gdb) disas main
Видим что есть команда сравнения 0x080484e7 <+83>:	cmp    $0x149c,%eax
Из параметров видно? что одно число $0x149c = 5276
Второе наш ввод (scanf(%d)) %eax (регистр общего назначения - для записи данных)

Для записи в читаемый код используем команду:
(gdb) x/s 0x8048636
где второй параметр (0x8048636) - это указатель на функцию или параметры, аргументы
Так выводим каждую строку исполняемой программы ./level00 в source.c

> ./level00
Вводим пароль:
Password:5276

Authenticated!

Смотрим кто мы:
> whoami
level01

Копируем ключ:
> cat /home/users/level01/.pass
uSq2ehEGT6c9S24zbshexZQBXUGrncxn5sD5QfGL

>su level01
Вводим наш ключ, который получили выше
Готово!


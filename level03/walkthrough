> ls -la
> ./level03
Выходит сообщение:

***********************************
*               level03         **
***********************************
Password:

Invalid Password

Видим, что от нас требуют ввода сообщения.
Дизасемблируем код и ищем момент считывания пароля.
Узнаем, что программа считывает пароль через scanf.
Видим, что это число и оно сравнивается с числом 21
Используя простую точку останова перед strcmp, мы видим, что она указывает на "Q}|u`sfg~sf{}|a3"

Перевод текста в число: 
https://www.rapidtables.com/convert/number/ascii-to-hex.html

Через gdb читаем что в ф-цию test подается конкретное число:
0x1337d00d или (int)322424845
В самой ф-ции видим, что нам нужно получить число не больше 21, которое мы получим путем вычитания из этого числа.
Поэтому перебираем все возможные числа в диапозоне (21 вариант):

> for i in range {322424824..322424845}; do (echo $i; cat -) | ./level03 ; done

На 5-ую попытку пароль засчитывается, далее вводим:

> cd ../level04
> cat .pass

Получаем пароль:
kgv3tkEb9h2mLkRsPkXRfc2mHbjMxQzvb2FrgKkf

> su level04


